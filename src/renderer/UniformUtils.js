import { Color } from '../math/Color';

class UniformUtils {
    static getValueFormat(value) {
        if (!value) {
            throw new Error('UniformUtils.getValueFormat: value is undefined');
        }
    }    
    
}

export { UniformUtils };